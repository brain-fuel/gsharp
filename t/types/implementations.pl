implements_trait(bool, un_op, bool_null).
implements_trait(bool, un_op, bool_id).
implements_trait(bool, un_op, bool_transfer).
implements_trait(bool, un_op, bool_not).

implements_trait(bool, bin_op, bool_nand).
